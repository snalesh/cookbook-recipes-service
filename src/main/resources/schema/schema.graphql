type Mutation {
    createRecipe(input: RecipeInput): Recipe
}

type Recipe {
    id: ID!
    name: String!
    description: String!
}

input RecipeInput {
    name: String!
    description: String!
}
